
Initialize
    - reserve space for the buffer array
    - reserve space for the sum
    - reserve space for the filename

Start
    check to make sure there are 2 arguments in the program (program filename)
    copy the filename to reserved space

    open the file in read only with a system call
    if that fails throw error and close program

    read the file into the buffer array
    if that fails throw error and close program


    set sum at 0
    parse each character in the buffer
        if the number is from 0 to 9 then build the number and add it to the sum
        else if its a newline then skip
    
    convert the sum into a string
    output that string to the console

    close the opened file

    exit the program